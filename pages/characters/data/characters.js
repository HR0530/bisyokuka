// 32キャラ分の雛形（ここを増やしてもOK・同じ画像を使い回し）
window.CHARACTERS = Array.from({length:32}, (_,i)=>({
  id:i,
  name:`キャラ #${i+1}`,
  rarity: (i%4)+1, // 1〜4
  // ここに個別画像が無ければデフォルトを使用
  img:"../assets/男_スーツ1.png",
  // 解放条件の例（任意で使える）
  unlockHint: i===0 ? "初期解放" :
              i%8===0 ? "ユニーク料理を増やそう" :
              i%5===0 ? "連続ログインを伸ばそう" :
                        "栄養バランスを整えよう"
}));
